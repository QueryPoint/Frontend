services:
  front:
    build: .
    container_name: querypoint_front
    restart: unless-stopped
    ports:
      - "3000:80"
    networks:
      - querypoint

networks:
  querypoint:
    external: true
    name: querypoint_network
